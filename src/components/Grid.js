import React, { Component, PropTypes } from 'react';


class Grid extends Component {
  static propTypes = {
  };

  render () {
    return (
      <div>This is a game grid.</div>
    );
  }
}


export default Grid;
